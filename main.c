#include <stdio.h>
// autre push pour test autograde
int main(void)
{
    int n;
    scanf("%d", &n);

    /* TODO : test if / else */
    if (n>=0)
    {
        printf("POSITIF c'est fou\n");
    }
    else
    {
        printf("NEGATIF\n");
    }
    return 0;
}
